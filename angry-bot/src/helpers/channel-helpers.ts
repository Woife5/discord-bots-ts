export class ChannelHelpers {
    /**
     * 
     * @param channelName 
     * @returns either channel id of stated (param) or current channel
     */
    static checkIfChannelExists(channelName : string) : number {
        return 0;
    }
}

