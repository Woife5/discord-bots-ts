{
    "name": "@woife5/frens-bot",
    "version": "0.1.1",
    "description": "Frens Bot",
    "type": "module",
    "license": "GPL-3.0",
    "author": "Wolfgang Schwendtbauer",
    "scripts": {
        "start": "pnpm run build && node --env-file=.env frens-bot.cjs",
        "build": "esbuild ./src/frens-bot.ts --bundle --outfile=frens-bot.cjs --minify-whitespace --minify-syntax --platform=node",
        "lint": "biome lint",
        "format": "biome format",
        "check": "biome check",
        "typecheck": "tsc --noEmit",
        "predockerize": "pnpm run lint && pnpm run typecheck && pnpm run build",
        "dockerize": "docker buildx build -t woife5/frens-bot:latest -f ./Dockerfile --platform=linux/arm64,linux/amd64 -o type=registry ."
    },
    "devDependencies": {
        "@biomejs/biome": "catalog:",
        "@types/node-cron": "^3.0.11",
        "typescript": "catalog:",
        "esbuild": "catalog:"
    },
    "dependencies": {
        "@woife5/shared": "workspace:*",
        "discord.js": "catalog:",
        "mongodb": "^7.0.0",
        "node-cron": "^4.2.1"
    }
}
