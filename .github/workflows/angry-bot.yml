name: Angry Bot

on:
    push:
        branches:
            - main
        paths:
            - "angry-bot/src/**"
    pull_request:
        branches:
            - main
        paths:
            - "angry-bot/src/**"

jobs:
    pipeline:
        uses: ./.github/workflows/bot-pipeline.yml
        with:
            bot-name: angry-bot
        secrets: inherit
